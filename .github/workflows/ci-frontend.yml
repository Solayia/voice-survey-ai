# =============================================================================
# CI — Frontend (PLACEHOLDER)
# -----------------------------------------------------------------------------
# Placeholder non fonctionnel. Les étapes réelles (installation, lint,
# vérification de types, build, tests) seront ajoutées une fois le frontend
# Next.js créé.
# =============================================================================
name: CI · Frontend

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - ".github/workflows/ci-frontend.yml"
  pull_request:
    paths:
      - "frontend/**"
      - ".github/workflows/ci-frontend.yml"

jobs:
  placeholder:
    name: Vérifications frontend (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Avis placeholder
        run: |
          echo "La CI frontend n'est pas encore configurée."
          echo "TODO : installer Node, les dépendances, lancer eslint + tsc + next build + tests."
