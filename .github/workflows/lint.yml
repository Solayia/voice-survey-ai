# =============================================================================
# Hygiène du dépôt (PLACEHOLDER)
# -----------------------------------------------------------------------------
# Placeholder non fonctionnel pour les vérifications transverses : lint Markdown,
# vérification des liens, conformité EditorConfig, détection de secrets, etc.
# =============================================================================
name: Hygiène du dépôt

on:
  push:
    branches: [main]
  pull_request:

jobs:
  placeholder:
    name: Vérifications d'hygiène (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Avis placeholder
        run: |
          echo "Les vérifications d'hygiène ne sont pas encore configurées."
          echo "TODO : markdownlint, vérification des liens, editorconfig-checker, scan de secrets."
