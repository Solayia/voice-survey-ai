# =============================================================================
# CI — Backend (PLACEHOLDER)
# -----------------------------------------------------------------------------
# Placeholder non fonctionnel. Les étapes réelles (installation, lint,
# vérification de types, tests) seront ajoutées une fois le backend FastAPI
# créé. Volontairement minimal pour câbler l'onglet Actions dès le départ.
# =============================================================================
name: CI · Backend

on:
  push:
    branches: [main]
    paths:
      - "backend/**"
      - ".github/workflows/ci-backend.yml"
  pull_request:
    paths:
      - "backend/**"
      - ".github/workflows/ci-backend.yml"

jobs:
  placeholder:
    name: Vérifications backend (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Avis placeholder
        run: |
          echo "La CI backend n'est pas encore configurée."
          echo "TODO : installer Python, les dépendances, lancer ruff + mypy + pytest."
