# =============================================================================
# CI — Backend (PLACEHOLDER)
# -----------------------------------------------------------------------------
# This is a non-functional placeholder. Real steps (install, lint, type-check,
# test) will be added once the FastAPI backend exists. Kept minimal so the
# Actions tab is wired up from day one.
# =============================================================================
name: CI · Backend

on:
  push:
    branches: [main]
    paths:
      - "backend/**"
      - ".github/workflows/ci-backend.yml"
  pull_request:
    paths:
      - "backend/**"
      - ".github/workflows/ci-backend.yml"

jobs:
  placeholder:
    name: Backend checks (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder notice
        run: |
          echo "Backend CI is not configured yet."
          echo "TODO: set up Python, install deps, run ruff + mypy + pytest."
